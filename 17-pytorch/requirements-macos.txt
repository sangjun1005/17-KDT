# macOS (Apple Silicon) + Python 3.11 용 버전 조합
# 책의 requirements.txt(2023년 기준, torch 2.0.1)는 arm64 / Python 3.12+ 에서 휠이 없어 설치되지 않습니다.
# 아래는 예제 코드 API를 유지하면서 실제 설치·실행이 검증된 버전입니다.
#
#   uv venv --python 3.11 .venv
#   uv pip install --python .venv/bin/python -r requirements-macos.txt

# --- 파이토치 스택 (torchtext 0.18이 마지막 릴리스라 torch 2.3.x로 고정) ---
numpy<2
torch==2.3.0
torchvision==0.18.0
torchtext==0.18.0
torchdata==0.8.0
torchinfo==1.8.0

# --- 허깅페이스 ---
transformers==4.44.2
tokenizers>=0.19,<0.20
datasets==2.21.0
evaluate==0.4.3
accelerate==0.33.0
huggingface-hub<0.25
sentencepiece==0.2.0
sacremoses==0.1.1
rouge_score
absl-py

# --- 데이터 / 학습 ---
pandas==2.2.2
scikit-learn==1.5.2
scipy<1.14
matplotlib==3.9.2
seaborn==0.13.2
pillow<11
lightning==2.4.0
tensorly==0.8.1

# --- 한국어 / NLP (konlpy는 JDK 필요) ---
konlpy==0.6.0
Korpora==0.2.0
nltk==3.9.1
gensim==4.3.3
jamo==0.4.1
nlpaug==1.1.11
spacy==3.7.5

# --- 비전 ---
opencv-python==4.10.0.84
pycocotools==2.0.8
ultralytics==8.2.103
imgaug==0.4.0
timm==1.0.9

# --- 배포 / 서빙 (11장) ---
onnx==1.16.2
onnxruntime==1.19.2
fastapi==0.115.0
uvicorn==0.30.6
flask==3.0.3
streamlit==1.38.0
requests
portalocker

# --- 노트북 ---
jupyter
ipykernel
ipywidgets
kaggle
