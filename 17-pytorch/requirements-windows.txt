# Windows (x64) + NVIDIA RTX 50 시리즈(Blackwell) + Python 3.11 용 버전 조합
# RTX 50 시리즈는 CUDA 12.8 이상 빌드(torch 2.7+)만 지원하므로 torch 2.3.x 대신 2.7.1+cu128을 사용합니다.
# torchtext는 torch 2.3.x까지만 동작(개발 종료)하므로 이 조합에서는 제외했습니다.
#
# 설치 (이 폴더의 .venv 에만 적용):
#   uv venv --python 3.11 .venv
#   uv pip install --python .venv\Scripts\python.exe torch==2.7.1 torchvision==0.22.1 --index-url https://download.pytorch.org/whl/cu128
#   uv pip install --python .venv\Scripts\python.exe -r requirements-windows.txt

# --- 파이토치 스택 (torch / torchvision 은 위 명령으로 CUDA 12.8 인덱스에서 먼저 설치) ---
numpy<2
torch==2.7.1
torchvision==0.22.1
torchinfo==1.8.0

# --- 허깅페이스 ---
transformers==4.44.2
tokenizers>=0.19,<0.20
datasets==2.21.0
evaluate==0.4.3
accelerate==0.33.0
huggingface-hub<0.25
sentencepiece==0.2.0
sacremoses==0.1.1
rouge_score
absl-py

# --- 데이터 / 학습 ---
pandas==2.2.2
scikit-learn==1.5.2
scipy<1.14
matplotlib==3.9.2
seaborn==0.13.2
pillow<11
lightning==2.4.0
tensorly==0.8.1

# --- 한국어 / NLP (konlpy는 JDK 필요) ---
konlpy==0.6.0
Korpora==0.2.0
nltk==3.9.1
gensim==4.3.3
jamo==0.4.1
nlpaug==1.1.11
spacy==3.7.5

# --- 비전 ---
opencv-python==4.10.0.84
pycocotools==2.0.8
ultralytics==8.2.103
imgaug==0.4.0
timm==1.0.9

# --- 배포 / 서빙 (11장) ---
onnx==1.16.2
onnxruntime==1.19.2
fastapi==0.115.0
uvicorn==0.30.6
flask==3.0.3
streamlit==1.38.0
requests
portalocker

# --- 노트북 ---
jupyter
ipykernel
ipywidgets
kaggle
